eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
       defaultZone: 'http://${discovery.server.hostName}:${discovery.server.port}/eureka'
      

discovery:
  server:
    port: 8070
    hostName: 18.119.40.220

management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include:
          - '*'